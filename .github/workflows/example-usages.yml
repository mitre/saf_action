name: Provide a few examples
on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test scan
      uses: mitre/saf_action@main
      with:
        command_string: 'scan'
