"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const core_1 = require("@oclif/core");
const fs_1 = __importDefault(require("fs"));
const hdf_converters_1 = require("@mitre/hdf-converters");
const lodash_1 = __importDefault(require("lodash"));
const global_1 = require("../../utils/global");
const base_command_1 = require("../../utils/oclif/base_command");
class Prisma2HDF extends base_command_1.BaseCommand {
    static usage = '<%= command.id %> -i <prisma-cloud-csv> -o <hdf-output-folder> [-h]';
    static description = 'Translate a Prisma Cloud Scan Report CSV file into Heimdall Data Format JSON files';
    static examples = ['<%= config.bin %> <%= command.id %> -i prismacloud-report.csv -o output-hdf-name.json'];
    static flags = {
        input: core_1.Flags.string({
            char: 'i',
            required: true,
            description: 'Prisma Cloud Scan Report CSV',
        }),
        output: core_1.Flags.string({
            char: 'o',
            required: true,
            description: 'Output HDF JSON File',
        }),
    };
    async run() {
        const { flags } = await this.parse(Prisma2HDF);
        const converter = new hdf_converters_1.PrismaMapper(fs_1.default.readFileSync(flags.input, { encoding: 'utf8' }));
        const results = converter.toHdf();
        if (!fs_1.default.existsSync(flags.output)) {
            fs_1.default.mkdirSync(flags.output);
        }
        lodash_1.default.forOwn(results, (result) => {
            fs_1.default.writeFileSync((0, global_1.resolveSafeChild)(flags.output, (0, global_1.safeFilename)(`${lodash_1.default.get(result, 'platform.target_id')}.json`)), JSON.stringify(result, null, 2));
        });
    }
}
exports.default = Prisma2HDF;
//# sourceMappingURL=prisma2hdf.js.map