import { BaseCommand } from '../../utils/oclif/baseCommand';
export default class Prowler2HDF extends BaseCommand<typeof Prowler2HDF> {
    static readonly usage = "<%= command.id %> -i <prowler-finding-json> -o <hdf-output-folder> [-h]";
    static readonly description = "Translate a Prowler-derived AWS Security Finding Format results from JSONL into a Heimdall Data Format JSON file";
    static readonly examples: string[];
    static readonly flags: {
        input: import("@oclif/core/interfaces").OptionFlag<string, import("@oclif/core/interfaces").CustomOptions>;
        output: import("@oclif/core/interfaces").OptionFlag<string, import("@oclif/core/interfaces").CustomOptions>;
    };
    run(): Promise<void>;
}
