"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const command_1 = require("@oclif/command");
const fs_1 = tslib_1.__importDefault(require("fs"));
const hdf_converters_1 = require("@mitre/hdf-converters");
const global_1 = require("../../utils/global");
class Sonarqube2HDF extends command_1.Command {
    async run() {
        const { flags } = this.parse(Sonarqube2HDF);
        const converter = new hdf_converters_1.SonarQubeResults(flags.url, flags.projectKey, flags.auth);
        fs_1.default.writeFileSync((0, global_1.checkSuffix)(flags.output), JSON.stringify(await converter.toHdf()));
    }
}
exports.default = Sonarqube2HDF;
Sonarqube2HDF.usage = 'convert:sonarqube2hdf -n <sonar_project_key> -u <http://your.sonar.instance:9000> --auth <your-sonar-api-key> -o <hdf-scan-results-json>';
Sonarqube2HDF.description = 'Pull SonarQube vulnerabilities for the specified project name from an API and convert into a Heimdall Data Format JSON file';
Sonarqube2HDF.examples = ['saf convert:sonarqube2hdf -n sonar_project_key -u http://sonar:9000 --auth YOUR_API_KEY -o scan_results.json'];
Sonarqube2HDF.flags = {
    help: command_1.flags.help({ char: 'h' }),
    auth: command_1.flags.string({ char: 'a', required: true }),
    projectKey: command_1.flags.string({ char: 'n', required: true }),
    url: command_1.flags.string({ char: 'u', required: true }),
    output: command_1.flags.string({ char: 'o', required: true }),
};
