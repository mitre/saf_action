"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = __importDefault(require("fs"));
const hdf_converters_1 = require("@mitre/hdf-converters");
const core_1 = require("@oclif/core");
const global_1 = require("../../utils/global");
class HDF2HTML extends core_1.Command {
    static usage = 'convert hdf2html -i <hdf-scan-results-json>... -o <output-html> [-t <output-type>] [-h]';
    static description = 'Translate an HDF file into a Heimdall Report HTML file';
    static examples = ['saf convert hdf2html -i hdf_input.json -o report.html -t Manager'];
    static flags = {
        help: core_1.Flags.help({ char: 'h' }),
        input: core_1.Flags.string({ char: 'i', required: true, multiple: true, description: 'Input HDF JSON file' }),
        output: core_1.Flags.string({ char: 'o', required: true, description: 'Output HTML file' }),
        type: core_1.Flags.string({ char: 't', default: hdf_converters_1.FileExportTypes.Administrator,
            description: 'The report type to generate\nReport types differ with the information they include\nExecutive: Profile Info + Statuses + Compliance Level\nManager: Executive + Test Results and Details\nAdministrator: Manager + Test Code',
            options: ['Executive', 'Manager', 'Administrator'] }),
    };
    async run() {
        const { flags } = await this.parse(HDF2HTML);
        const files = flags.input.map((file, i) => ({ data: fs_1.default.readFileSync(file, 'utf8'), fileName: (0, global_1.basename)(file), fileID: `${i}` }));
        const converter = await new hdf_converters_1.FromHDFToHTMLMapper(files, hdf_converters_1.FileExportTypes[flags.type]).toHTML();
        fs_1.default.writeFileSync(flags.output, converter);
    }
}
exports.default = HDF2HTML;
//# sourceMappingURL=hdf2html.js.map