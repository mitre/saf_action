"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const core_1 = require("@oclif/core");
const fs_1 = tslib_1.__importDefault(require("fs"));
const hdf_converters_1 = require("@mitre/hdf-converters");
const global_1 = require("../../utils/global");
class Twistlock2HDF extends core_1.Command {
    static usage = 'convert twistlock2hdf -i <twistlock-json> -o <hdf-scan-results-json>';
    static description = 'Translate a Twistlock CLI output file into an HDF results set';
    static examples = ['saf convert twistlock2hdf -i twistlock.json -o output-hdf-name.json'];
    static flags = {
        help: core_1.Flags.help({ char: 'h' }),
        input: core_1.Flags.string({ char: 'i', required: true, description: 'Input Twistlock file' }),
        output: core_1.Flags.string({ char: 'o', required: true, description: 'Output HDF JSON File' }),
    };
    async run() {
        const { flags } = await this.parse(Twistlock2HDF);
        // Check for correct input type
        const data = fs_1.default.readFileSync(flags.input, 'utf8');
        (0, global_1.checkInput)({ data: data, filename: flags.input }, 'twistlock', 'Twistlock CLI output file');
        const converter = new hdf_converters_1.TwistlockResults(data);
        fs_1.default.writeFileSync((0, global_1.checkSuffix)(flags.output), JSON.stringify(converter.toHdf()));
    }
}
exports.default = Twistlock2HDF;
