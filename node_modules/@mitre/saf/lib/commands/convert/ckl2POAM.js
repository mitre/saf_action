"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const promises_1 = require("fs/promises");
const core_1 = require("@oclif/core");
const fast_xml_parser_1 = require("fast-xml-parser");
const lodash_1 = __importDefault(require("lodash"));
const moment_1 = __importDefault(require("moment"));
const prompt_sync_1 = __importDefault(require("prompt-sync"));
const xlsx_populate_1 = __importDefault(require("xlsx-populate"));
const files_json_1 = __importDefault(require("../../resources/files.json"));
const ckl2poam_1 = require("../../utils/ckl2poam");
const global_1 = require("../../utils/global");
const logging_1 = require("../../utils/logging");
const base_command_1 = require("../../utils/oclif/base_command");
const prompt = (0, prompt_sync_1.default)();
const STARTING_ROW = 8; // The row we start inserting controls into
class CKL2POAM extends base_command_1.BaseCommand {
    static usage = '<%= command.id %> -i <disa-checklist>... -o <poam-output-folder> [-h] [-O <office/org>] [-d <device-name>] [-s <num-rows>]';
    static description = 'Translate DISA Checklist CKL file(s) to POA&M files';
    static aliases = ['convert:ckl2POAM'];
    static examples = ['<%= config.bin %> <%= command.id %> -i checklist_file.ckl -o output-folder -d abcdefg -s 2'];
    static flags = {
        help: core_1.Flags.help({ char: 'h' }),
        input: core_1.Flags.string({
            char: 'i',
            required: true,
            multiple: true,
            description: 'Path to the DISA Checklist File(s)',
        }),
        officeOrg: core_1.Flags.string({
            char: 'O',
            required: false,
            default: '',
            description: 'Default value for Office/org (prompts for each file if not set)',
        }),
        deviceName: core_1.Flags.string({
            char: 'd',
            required: false,
            default: '',
            description: 'Name of target device (prompts for each file if not set)',
        }),
        rowsToSkip: core_1.Flags.integer({
            char: 's',
            required: false,
            default: 4,
            description: 'Rows to leave between POA&M Items for milestones',
        }),
        output: core_1.Flags.string({
            char: 'o',
            required: true,
            description: 'Path to output PO&M File(s)',
        }),
    };
    async run() {
        const { flags } = await this.parse(CKL2POAM);
        const logger = (0, logging_1.createWinstonLogger)({ module: 'ckl2poam', level: flags.logLevel });
        const officeOrgsAndDeviceNames = Object.fromEntries(flags.input.map((fileName) => {
            let officeOrg = flags.officeOrg;
            if (!officeOrg) {
                officeOrg = prompt(`What should the default value be for Office/org for ${fileName}? `);
            }
            let deviceName = flags.deviceName;
            if (!deviceName) {
                deviceName = prompt(`What is the device name for ${fileName}? `);
            }
            return [fileName, { officeOrg, deviceName }];
        }));
        await (0, promises_1.mkdir)(flags.output, { recursive: true });
        await Promise.all(flags.input.map(async (fileName) => {
            // Ignore files that start with . (e.g .gitignore)
            if (fileName.startsWith('.')) {
                return;
            }
            logger.info(`${fileName}: Opening file`);
            let data;
            try {
                data = await (0, promises_1.readFile)(fileName, { encoding: 'utf8' });
            }
            catch (readFileError) {
                logger.error(`${fileName}: An error occurred opening the file ${fileName}: ${readFileError instanceof Error ? readFileError.message : JSON.stringify(readFileError)}`);
                throw readFileError;
            }
            let result;
            // https://stackoverflow.com/a/79876267 - parser options for fast-xml-parser that match xml2js' behavior; can look into removing exact compability later
            const parser = new fast_xml_parser_1.XMLParser({
                ignoreAttributes: false,
                ignoreDeclaration: true,
                attributeNamePrefix: '',
                attributesGroupName: '$',
                textNodeName: '_',
                parseTagValue: false,
                parseAttributeValue: false,
                isArray: (name, jpath, isLeafNode, isAttribute) => {
                    return !isAttribute && lodash_1.default.isString(jpath) && jpath.includes('.');
                },
                trimValues: false,
                tagValueProcessor: (_tagName, tagValue) => {
                    if (typeof tagValue === 'string' && tagValue.includes('\n') && tagValue.trim().length === 0) {
                        return '';
                    }
                    return tagValue;
                },
            });
            try {
                result = parser.parse(data);
            }
            catch (parseFileError) {
                logger.error(`${fileName}: An error occurred parsing the file: ${parseFileError instanceof Error ? parseFileError.message : JSON.stringify(parseFileError)}`);
                throw parseFileError;
            }
            const infos = {};
            let vulnerabilities = [];
            const iStigs = [];
            const stigs = result.CHECKLIST.STIGS;
            logger.info(`${fileName}: Found ${stigs?.length} STIGs`);
            // Get nested iSTIGs
            if (stigs) {
                for (const stig of stigs) {
                    if (stig.iSTIG) {
                        for (const iStig of stig.iSTIG) {
                            iStigs.push(iStig);
                        }
                    }
                }
            }
            logger.info(`${fileName}: Found ${iStigs.length} iSTIGs`);
            // Get the controls/vulnerabilities from each stig
            for (const iSTIG of iStigs) {
                if (iSTIG.STIG_INFO) {
                    for (const info of iSTIG.STIG_INFO) {
                        if (info.SI_DATA) {
                            for (const data of info.SI_DATA) {
                                if (data.SID_DATA) {
                                    infos[data.SID_NAME[0]] = data.SID_DATA[0];
                                }
                            }
                        }
                    }
                }
                if (iSTIG.VULN) {
                    vulnerabilities = [
                        ...vulnerabilities,
                        ...iSTIG.VULN.map((vulnerability) => {
                            const values = {};
                            // Extract STIG_DATA
                            if (vulnerability.STIG_DATA) {
                                for (const data of vulnerability.STIG_DATA.toReversed()) {
                                    values[data.VULN_ATTRIBUTE[0]] = data.ATTRIBUTE_DATA[0];
                                }
                            }
                            // Extract remaining fields (status, finding details, comments, security override, and severity justification)
                            for (const [key, value] of Object.entries(vulnerability)) {
                                values[key] = value?.[0];
                            }
                            return values;
                        }),
                    ];
                }
            }
            logger.info(`${fileName}: Found ${vulnerabilities.length} vulnerabilities`);
            const officeOrg = officeOrgsAndDeviceNames[fileName].officeOrg;
            const deviceName = officeOrgsAndDeviceNames[fileName].deviceName;
            // Read our template
            const workBook = await xlsx_populate_1.default.fromDataAsync((0, global_1.dataURLtoU8Array)(files_json_1.default.POAMTemplate.data));
            // eMASS reads the first sheet in the notebook
            const sheet = workBook.sheet(0);
            // The current row we are on
            let currentRow = STARTING_ROW;
            // The scheduled completion date, default of one year from today
            const aYearFromNow = (0, moment_1.default)(new Date(new Date().setFullYear(new Date().getFullYear() + 1))).format('M/DD/YYYY');
            // For each vulnerability
            for (const vulnerability of vulnerabilities) {
                if (vulnerability.STATUS !== 'NotAFinding'
                    && vulnerability.STATUS !== 'Not_Reviewed') {
                    // Control Vulnerability Description
                    if (vulnerability.STATUS === 'Not_Applicable') {
                        sheet.cell(`C${currentRow}`).value('Not Applicable');
                    }
                    else {
                        sheet
                            .cell(`C${currentRow}`)
                            .value((0, ckl2poam_1.replaceSpecialCharacters)((0, ckl2poam_1.createCVD)(vulnerability)));
                    }
                    // Security Control Number
                    sheet
                        .cell(`D${currentRow}`)
                        .value((0, ckl2poam_1.cci2nist)(vulnerability.CCI_REF || ''));
                    // Office/org
                    sheet.cell(`E${currentRow}`).value(officeOrg);
                    // Security Checks
                    sheet
                        .cell(`F${currentRow}`)
                        .value(vulnerability.Rule_ID?.split(',')[0]);
                    // Resources Required
                    sheet.cell(`G${currentRow}`).value('NA');
                    // Scheduled Completion Date
                    // Default is one year from today
                    sheet.cell(`H${currentRow}`).value(aYearFromNow);
                    // Source Identifying Vulnerability
                    sheet.cell(`K${currentRow}`).value(infos.title || '');
                    // Status
                    sheet
                        .cell(`L${currentRow}`)
                        .value((0, ckl2poam_1.cleanStatus)(vulnerability.STATUS || ''));
                    // Comments
                    if (vulnerability.STATUS === 'Open'
                        || vulnerability.STATUS === 'Not_Applicable') {
                        if (deviceName.startsWith('Nessus')) {
                            sheet
                                .cell(`M${currentRow}`)
                                .value((0, ckl2poam_1.combineComments)(vulnerability, (0, ckl2poam_1.extractSTIGUrl)(vulnerability.FINDING_DETAILS || '')));
                        }
                        else {
                            sheet
                                .cell(`M${currentRow}`)
                                .value((0, ckl2poam_1.combineComments)(vulnerability, deviceName));
                        }
                    }
                    // Raw Severity
                    sheet
                        .cell(`N${currentRow}`)
                        .value((0, ckl2poam_1.convertToRawSeverity)(vulnerability.Severity || ''));
                    // Severity
                    sheet
                        .cell(`P${currentRow}`)
                        .value((0, ckl2poam_1.cklSeverityToPOAMSeverity)(vulnerability.Severity || ''));
                    // Relevance of Threat
                    sheet
                        .cell(`Q${currentRow}`)
                        .value((0, ckl2poam_1.cklSeverityToRelevanceOfThreat)());
                    // Likelihood
                    sheet
                        .cell(`R${currentRow}`)
                        .value((0, ckl2poam_1.cklSeverityToLikelihood)(vulnerability.Severity || ''));
                    // Impact
                    sheet
                        .cell(`S${currentRow}`)
                        .value((0, ckl2poam_1.cklSeverityToImpact)(vulnerability.Severity || ''));
                    // Residual Risk Level
                    sheet
                        .cell(`U${currentRow}`)
                        .value((0, ckl2poam_1.cklSeverityToResidualRiskLevel)(vulnerability.Severity || ''));
                    // Impact Description
                    sheet
                        .cell(`T${currentRow}`)
                        .value((0, ckl2poam_1.replaceSpecialCharacters)(vulnerability.Vuln_Discuss || ''));
                    // Recommendations
                    sheet
                        .cell(`V${currentRow}`)
                        .value((0, ckl2poam_1.replaceSpecialCharacters)(vulnerability.Fix_Text
                        || (0, ckl2poam_1.extractSolution)(vulnerability.FINDING_DETAILS || '')
                        || ''));
                    // Go to the next row
                    currentRow += flags.rowsToSkip + 1;
                }
            }
            return workBook.toFileAsync((0, global_1.resolveSafeChild)(flags.output, (0, global_1.safeFilename)(`${(0, global_1.basename)(fileName)}-${(0, moment_1.default)(new Date()).format('YYYY-MM-DD-HHmm')}.xlsm`)));
        }));
    }
}
exports.default = CKL2POAM;
//# sourceMappingURL=ckl2poam.js.map