"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const core_1 = require("@oclif/core");
const fs_1 = __importDefault(require("fs"));
const hdf_converters_1 = require("@mitre/hdf-converters");
const global_1 = require("../../utils/global");
const base_command_1 = require("../../utils/oclif/base_command");
class CKL2HDF extends base_command_1.BaseCommand {
    static usage = '<%= command.id %> -i <ckl-xml> -o <hdf-scan-results-json> [-r]';
    static description = 'Translate a Checklist XML file into a Heimdall Data Format JSON file';
    static examples = ['<%= config.bin %> <%= command.id %> -i ckl_results.xml -o output-hdf-name.json'];
    static flags = {
        input: core_1.Flags.string({
            char: 'i',
            required: true,
            description: 'Input Checklist XML File',
        }),
        output: core_1.Flags.string({
            char: 'o',
            required: true,
            description: 'Output HDF JSON File',
        }),
        includeRaw: core_1.Flags.boolean({
            char: 'r',
            required: false,
            description: 'Include raw input file in HDF JSON file',
        }),
    };
    async run() {
        const { flags } = await this.parse(CKL2HDF);
        const data = fs_1.default.readFileSync(flags.input, 'utf8');
        (0, global_1.checkInput)({ data, filename: flags.input }, hdf_converters_1.INPUT_TYPES.CHECKLIST, 'DISA Checklist');
        try {
            const converter = new hdf_converters_1.ChecklistResults(data, flags.includeRaw);
            fs_1.default.writeFileSync((0, global_1.checkSuffix)(flags.output), JSON.stringify(converter.toHdf(), null, 2));
        }
        catch (error) {
            console.error(`Error converting to hdf:\n${error instanceof Error ? error.message : JSON.stringify(error, null, 2)}`);
        }
    }
}
exports.default = CKL2HDF;
//# sourceMappingURL=ckl2hdf.js.map