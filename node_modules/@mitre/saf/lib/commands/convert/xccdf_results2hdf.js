"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = __importDefault(require("fs"));
const hdf_converters_1 = require("@mitre/hdf-converters");
const core_1 = require("@oclif/core");
const global_1 = require("../../utils/global");
const base_command_1 = require("../../utils/oclif/base_command");
class XCCDFResults2HDF extends base_command_1.BaseCommand {
    static usage = '<%= command.id %> -i <xccdf-results-xml> -o <hdf-scan-results-json> [-h] [-w]';
    static description = 'Translate a SCAP client XCCDF-Results XML report to a Heimdall Data Format JSON file';
    static examples = ['<%= config.bin %> <%= command.id %> -i results-xccdf.xml -o output-hdf-name.json'];
    static flags = {
        input: core_1.Flags.string({
            char: 'i',
            required: true,
            description: 'Input XCCDF Results XML File',
        }),
        output: core_1.Flags.string({
            char: 'o',
            required: true,
            description: 'Output HDF JSON File',
        }),
        includeRaw: core_1.Flags.boolean({
            char: 'w',
            required: false,
            description: 'Include raw input file in HDF JSON file',
        }),
    };
    async run() {
        const { flags } = await this.parse(XCCDFResults2HDF);
        // Check for correct input type
        const data = fs_1.default.readFileSync(flags.input, 'utf8');
        (0, global_1.checkInput)({ data, filename: flags.input }, hdf_converters_1.INPUT_TYPES.XCCDF, 'SCAP client XCCDF-Results XML report');
        const converter = new hdf_converters_1.XCCDFResultsResults(data, flags.includeRaw);
        fs_1.default.writeFileSync((0, global_1.checkSuffix)(flags.output), JSON.stringify(await converter.toHdf(), null, 2));
    }
}
exports.default = XCCDFResults2HDF;
//# sourceMappingURL=xccdf_results2hdf.js.map