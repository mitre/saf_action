"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = __importDefault(require("fs"));
const https_1 = __importDefault(require("https"));
const path_1 = __importDefault(require("path"));
const client_securityhub_1 = require("@aws-sdk/client-securityhub");
const hdf_converters_1 = require("@mitre/hdf-converters");
const core_1 = require("@oclif/core");
const node_http_handler_1 = require("@smithy/node-http-handler");
const lodash_1 = __importDefault(require("lodash"));
const global_1 = require("../../utils/global");
const base_command_1 = require("../../utils/oclif/base_command");
class HDF2ASFF extends base_command_1.BaseCommand {
    static usage = '<%= command.id %> -a <account-id> -r <region> -i <hdf-scan-results-json> -t <target> [-h] [-R] (-u [-I -C <certificate>] | [-o <asff-output-folder>])';
    static description = 'Translate a Heimdall Data Format JSON file into AWS Security Findings Format JSON file(s) and/or upload to AWS Security Hub';
    static examples = [
        {
            description: '\u001B[93mSend output to local file system\u001B[0m',
            command: '<%= config.bin %> <%= command.id %> -i rhel7-scan_02032022A.json -a 123456789 -r us-east-1 -t rhel7_example_host -o rhel7.asff',
        },
        {
            description: '\u001B[93mUpload findings to AWS Security Hub\u001B[0m',
            command: '<%= config.bin %> <%= command.id %> -i rds_mysql_i123456789scan_03042022A.json -a 987654321 -r us-west-1 -t Instance_i123456789 -u',
        },
        {
            description: '\u001B[93mUpload findings to AWS Security Hub and Send output to local file system\u001B[0m',
            command: '<%= config.bin %> <%= command.id %> -i snyk_acme_project5_hdf_04052022A.json -a 2143658798 -r us-east-1 -t acme_project5 -o snyk_acme_project5 -u',
        },
    ];
    static flags = {
        accountId: core_1.Flags.string({
            char: 'a',
            required: true,
            description: 'AWS Account ID',
        }),
        region: core_1.Flags.string({
            char: 'r',
            required: true,
            description: 'SecurityHub Region',
        }),
        specifyRegionAttribute: core_1.Flags.boolean({
            char: 'R',
            required: false,
            description: 'Manually specify the top-level `Region` attribute - SecurityHub populates this attribute automatically and prohibits one from updating it using `BatchImportFindings` or `BatchUpdateFindings`',
        }),
        input: core_1.Flags.string({
            char: 'i',
            required: true,
            description: 'Input HDF JSON File',
        }),
        target: core_1.Flags.string({
            char: 't',
            required: true,
            description: 'Unique name for target to track findings across time',
        }),
        upload: core_1.Flags.boolean({
            char: 'u',
            required: false,
            description: 'Upload findings to AWS Security Hub',
        }),
        output: core_1.Flags.string({
            char: 'o',
            required: false,
            description: 'Output ASFF JSON Folder',
        }),
        insecure: core_1.Flags.boolean({
            char: 'I',
            required: false,
            default: false,
            description: 'Disable SSL verification, this is insecure.',
        }),
        certificate: core_1.Flags.string({
            char: 'C',
            required: false,
            description: 'Trusted signing certificate file',
        }),
    };
    async run() {
        const { flags } = await this.parse(HDF2ASFF);
        const converted = new hdf_converters_1.FromHdfToAsffMapper(JSON.parse(fs_1.default.readFileSync(flags.input, 'utf8')), {
            awsAccountId: flags.accountId,
            region: flags.region,
            regionAttribute: flags.specifyRegionAttribute,
            target: flags.target,
            input: flags.input,
        }).toAsff();
        if (flags.output) {
            const convertedSlices = lodash_1.default.chunk(converted, 100); // AWS doesn't allow uploading more than 100 findings at a time so we need to split them into chunks
            const outputFolder = flags.output.replace('.json', '') || 'asff-output';
            fs_1.default.mkdirSync(outputFolder);
            if (convertedSlices.length === 1) {
                const outfilePath = path_1.default.join(outputFolder, (0, global_1.checkSuffix)((0, global_1.basename)(flags.output)));
                fs_1.default.writeFileSync(outfilePath, JSON.stringify(convertedSlices[0], null, 2));
            }
            else {
                for (const [index, slice] of convertedSlices.entries()) {
                    const outfilePath = path_1.default.join(outputFolder, `${(0, global_1.checkSuffix)((0, global_1.basename)(flags.output || '')).replace('.json', '')}.p${index}.json`);
                    fs_1.default.writeFileSync(outfilePath, JSON.stringify(slice, null, 2));
                }
            }
        }
        if (flags.upload) {
            const profileInfoFinding = converted.pop();
            const convertedSlices = lodash_1.default.chunk(converted, 100);
            if (flags.insecure) {
                console.warn('WARNING: Using --insecure will make all connections to AWS open to MITM attacks, if possible pass a certificate file with --certificate');
            }
            const clientOptions = {
                region: flags.region,
                requestHandler: new node_http_handler_1.NodeHttpHandler({
                    httpsAgent: new https_1.default.Agent({
                        // Disable HTTPS verification if requested
                        rejectUnauthorized: !flags.insecure,
                        // Pass an SSL certificate to trust
                        ca: flags.certificate
                            ? fs_1.default.readFileSync(flags.certificate, 'utf8')
                            : undefined,
                    }),
                }),
            };
            const client = new client_securityhub_1.SecurityHub(clientOptions);
            try {
                await Promise.all(convertedSlices.map(async (chunk) => {
                    try {
                        const result = await client.batchImportFindings({
                            Findings: chunk,
                        });
                        console.log(`Uploaded ${chunk.length} controls. Success: ${result.SuccessCount}, Fail: ${result.FailedCount}`);
                        if (result.FailedFindings?.length) {
                            console.error(`Failed to upload ${result.FailedCount} Findings`);
                            console.log(result.FailedFindings);
                        }
                    }
                    catch (error) {
                        if (lodash_1.default.isObject(error)
                            && lodash_1.default.get(error, 'code') === 'NetworkingError') {
                            console.error(`Failed to upload controls: ${error instanceof Error ? error.message : JSON.stringify(error, null, 2)}; Using --certificate to provide your own SSL intermediary certificate (in .crt format) or use the flag --insecure to ignore SSL might resolve this issue`);
                        }
                        else {
                            console.error(`Failed to upload controls: ${error instanceof Error ? error.message : JSON.stringify(error, null, 2)}`);
                        }
                    }
                }));
            }
            catch (error) {
                if (error instanceof Error) {
                    console.error(error.message);
                }
                else {
                    console.error('Unexpected error', error);
                }
            }
            try {
                if (profileInfoFinding) {
                    profileInfoFinding.UpdatedAt = new Date().toISOString();
                    const result = await client.batchImportFindings({
                        Findings: [profileInfoFinding],
                    });
                    console.info(`Statistics: ${profileInfoFinding.Description}`);
                    console.info(`Uploaded Results Set Info Finding(s) - Success: ${result.SuccessCount}, Fail: ${result.FailedCount}`);
                    if (result.FailedFindings?.length) {
                        console.error(`Failed to upload ${result.FailedCount} Results Set Info Finding`);
                        console.log(result.FailedFindings);
                    }
                }
            }
            catch (error) {
                if (error instanceof Error) {
                    console.error(error.message);
                }
                else {
                    console.error('Unexpected error', error);
                }
            }
        }
    }
}
exports.default = HDF2ASFF;
//# sourceMappingURL=hdf2asff.js.map