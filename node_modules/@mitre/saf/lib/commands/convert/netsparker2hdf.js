"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const command_1 = require("@oclif/command");
const fs_1 = tslib_1.__importDefault(require("fs"));
const hdf_converters_1 = require("@mitre/hdf-converters");
const global_1 = require("../../utils/global");
class Netsparker2HDF extends command_1.Command {
    async run() {
        const { flags } = this.parse(Netsparker2HDF);
        const converter = new hdf_converters_1.NetsparkerMapper(fs_1.default.readFileSync(flags.input, 'utf-8'));
        fs_1.default.writeFileSync((0, global_1.checkSuffix)(flags.output), JSON.stringify(converter.toHdf()));
    }
}
exports.default = Netsparker2HDF;
Netsparker2HDF.usage = 'convert netsparker2hdf -i, --input=XML -o, --output=OUTPUT';
Netsparker2HDF.description = 'Translate a Netsparker XML results file into a Heimdall Data Format JSON file\nThe current iteration only works with Netsparker Enterprise Vulnerabilities Scan.';
Netsparker2HDF.examples = ['saf convert:netsparker2hdf -i netsparker_results.xml -o output-hdf-name.json'];
Netsparker2HDF.flags = {
    help: command_1.flags.help({ char: 'h' }),
    input: command_1.flags.string({ char: 'i', required: true }),
    output: command_1.flags.string({ char: 'o', required: true }),
};
