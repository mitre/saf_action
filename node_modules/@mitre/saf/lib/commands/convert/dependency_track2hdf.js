"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const core_1 = require("@oclif/core");
const fs_1 = __importDefault(require("fs"));
const hdf_converters_1 = require("@mitre/hdf-converters");
const global_1 = require("../../utils/global");
const base_command_1 = require("../../utils/oclif/base_command");
class DependencyTrack2HDF extends base_command_1.BaseCommand {
    static usage = '<%= command.id %> -i <dt-fpf-json> -o <hdf-scan-results-json> [-h] [-w]';
    static description = 'Translate a Dependency-Track results JSON file into a Heimdall Data Format JSON file';
    static examples = ['<%= config.bin %> <%= command.id %> -i dt-fpf.json -o output-hdf-name.json'];
    static flags = {
        input: core_1.Flags.string({ char: 'i', required: true, description: 'Input Dependency-Track FPF file' }),
        output: core_1.Flags.string({ char: 'o', required: true, description: 'Output HDF file' }),
        'with-raw': core_1.Flags.boolean({ char: 'w', required: false }),
    };
    async run() {
        const { flags } = await this.parse(DependencyTrack2HDF);
        const data = fs_1.default.readFileSync(flags.input, 'utf8');
        (0, global_1.checkInput)({ data, filename: flags.input }, hdf_converters_1.INPUT_TYPES.DEPENDENCY_TRACK, 'Dependency-Track results JSON');
        const converter = new hdf_converters_1.DependencyTrackMapper(data, flags['with-raw']);
        fs_1.default.writeFileSync((0, global_1.checkSuffix)(flags.output), JSON.stringify(converter.toHdf(), null, 2));
    }
}
exports.default = DependencyTrack2HDF;
//# sourceMappingURL=dependency_track2hdf.js.map