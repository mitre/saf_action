"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const command_1 = require("@oclif/command");
const fs = tslib_1.__importStar(require("fs"));
const hdf_converters_1 = require("@mitre/hdf-converters");
const global_1 = require("../../utils/global");
class Fortify2HDF extends command_1.Command {
    async run() {
        const { flags } = this.parse(Fortify2HDF);
        const converter = new hdf_converters_1.FortifyMapper(fs.readFileSync(flags.input, 'utf-8'));
        fs.writeFileSync((0, global_1.checkSuffix)(flags.output), JSON.stringify(converter.toHdf()));
    }
}
exports.default = Fortify2HDF;
Fortify2HDF.usage = 'convert:fortify2hdf -i, --input=FVDL -o, --output=OUTPUT';
Fortify2HDF.description = 'Translate a Fortify results FVDL file into a Heimdall Data Format JSON file';
Fortify2HDF.examples = ['saf convert:fortify2hdf -i audit.fvdl -o output-hdf-name.json'];
Fortify2HDF.flags = {
    help: command_1.flags.help({ char: 'h' }),
    input: command_1.flags.string({ char: 'i', required: true }),
    output: command_1.flags.string({ char: 'o', required: true }),
};
