"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const core_1 = require("@oclif/core");
const fs_1 = tslib_1.__importDefault(require("fs"));
const hdf_converters_1 = require("@mitre/hdf-converters");
const global_1 = require("../../utils/global");
class JfrogXray2HDF extends core_1.Command {
    async run() {
        const { flags } = await this.parse(JfrogXray2HDF);
        const converter = new hdf_converters_1.JfrogXrayMapper(fs_1.default.readFileSync(flags.input, 'utf-8'));
        fs_1.default.writeFileSync((0, global_1.checkSuffix)(flags.output), JSON.stringify(converter.toHdf()));
    }
}
exports.default = JfrogXray2HDF;
JfrogXray2HDF.usage = 'convet:jfrog_xray2hdf -i, --input=JSON -o, --output=OUTPUT';
JfrogXray2HDF.description = 'Translate a JFrog Xray results JSON file into a Heimdall Data Format JSON file';
JfrogXray2HDF.examples = ['saf convert jfrog_xray2hdf -i xray_results.json -o output-hdf-name.json'];
JfrogXray2HDF.flags = {
    help: core_1.Flags.help({ char: 'h' }),
    input: core_1.Flags.string({ char: 'i', required: true }),
    output: core_1.Flags.string({ char: 'o', required: true }),
};
