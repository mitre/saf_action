"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const core_1 = require("@oclif/core");
const fs_1 = tslib_1.__importDefault(require("fs"));
const hdf_converters_1 = require("@mitre/hdf-converters");
const global_1 = require("../../utils/global");
class Burpsuite2HDF extends core_1.Command {
    static usage = 'convert burpsuite2hdf -i <burpsuite-xml> -o <hdf-scan-results-json> [-h] [-w]';
    static description = 'Translate a BurpSuite Pro XML file into a Heimdall Data Format JSON file';
    static examples = ['saf convert burpsuite2hdf -i burpsuite_results.xml -o output-hdf-name.json'];
    static flags = {
        help: core_1.Flags.help({ char: 'h' }),
        input: core_1.Flags.string({ char: 'i', required: true, description: 'Input Burpsuite Pro XML File' }),
        output: core_1.Flags.string({ char: 'o', required: true, description: 'Output HDF JSON File' }),
        'with-raw': core_1.Flags.boolean({ char: 'w', required: false, description: 'Include raw input file in HDF JSON file' }),
    };
    async run() {
        const { flags } = await this.parse(Burpsuite2HDF);
        // Check for correct input type
        const data = fs_1.default.readFileSync(flags.input, 'utf8');
        (0, global_1.checkInput)({ data, filename: flags.input }, 'burp', 'BurpSuite Pro XML');
        const converter = new hdf_converters_1.BurpSuiteMapper(data, flags['with-raw']);
        fs_1.default.writeFileSync((0, global_1.checkSuffix)(flags.output), JSON.stringify(converter.toHdf()));
    }
}
exports.default = Burpsuite2HDF;
//# sourceMappingURL=burpsuite2hdf.js.map