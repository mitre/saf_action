import { BaseCommand } from '../../utils/oclif/baseCommand';
export default class HDF2CAAT extends BaseCommand<typeof HDF2CAAT> {
    static readonly usage = "<%= command.id %> -i <hdf-scan-results-json>... -o <output-caat-xlsx> [-h]";
    static readonly description = "Translate an HDF file into a Compliance Assessment and Audit Tracking (CAAT) XLSX file";
    static readonly examples: string[];
    static readonly flags: {
        input: import("@oclif/core/interfaces").OptionFlag<string[], import("@oclif/core/interfaces").CustomOptions>;
        output: import("@oclif/core/interfaces").OptionFlag<string, import("@oclif/core/interfaces").CustomOptions>;
    };
    run(): Promise<void>;
}
