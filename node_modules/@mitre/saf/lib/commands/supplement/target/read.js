"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const core_1 = require("@oclif/core");
const fs_1 = tslib_1.__importDefault(require("fs"));
class ReadTarget extends core_1.Command {
    async run() {
        const { flags } = await this.parse(ReadTarget);
        const input = JSON.parse(fs_1.default.readFileSync(flags.input, 'utf8'));
        const target = input.target || {};
        if (flags.output) {
            fs_1.default.writeFileSync(flags.output, JSON.stringify(target, null, 2));
        }
        else {
            process.stdout.write(JSON.stringify(target, null, 2));
        }
    }
}
exports.default = ReadTarget;
ReadTarget.usage = 'supplement target read -i <hdf-json> [-o <target-json>]';
ReadTarget.description = 'Read the `target` attribute in a given Heimdall Data Format JSON file and send it to stdout or write it to a file';
ReadTarget.examples = ['saf supplement target read -i hdf.json -o target.json'];
ReadTarget.flags = {
    help: core_1.Flags.help({ char: 'h' }),
    input: core_1.Flags.string({ char: 'i', required: true, description: 'An input HDF file' }),
    output: core_1.Flags.string({ char: 'o', description: 'An output `target` JSON file (otherwise the data is sent to stdout)' }),
};
