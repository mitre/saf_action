"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const emass_client_1 = require("@mitre/emass_client");
const core_1 = require("@oclif/core");
const json_colorizer_1 = require("json-colorizer");
const api_connection_1 = require("../../../utils/emasser/api_connection");
const output_formatter_1 = require("../../../utils/emasser/output_formatter");
const utilities_1 = require("../../../utils/emasser/utilities");
const endpoint = 'workflow_instances';
class EmasserGetWorkflowInstances extends core_1.Command {
    static usage = '<%= command.id %> [ARGUMENT] [FLAGS]\n \u001B[93m NOTE: see EXAMPLES for argument case format\u001B[0m';
    static description = (0, utilities_1.getDescriptionForEndpoint)(process.argv, endpoint);
    static examples = (0, utilities_1.getExamplesForEndpoint)(process.argv);
    static flags = {
        help: core_1.Flags.help({ char: 'h', description: 'Show eMASSer CLI help for the GET Workflow Instances command' }),
        ...(0, utilities_1.getFlagsForEndpoint)(process.argv),
    };
    // NOTE: The way args are being implemented are mainly for clarity purposes, there is, it displays
    //       the available arguments with associate description.
    // Only args.name is used, there is, it contains the argument listed by the user.
    // Example: If the user uses the command (saf emasser get workflow_instances byInstanceId), args.name is set to byInstanceId
    static args = {
        name: core_1.Args.string({ name: 'name', required: false, hidden: true }),
        all: core_1.Args.string({ name: 'all', description: 'Retrieves all workflow instances in a site', required: false }),
        byInstanceId: core_1.Args.string({ name: 'byInstanceId', description: 'Retrieves workflow(s) instance by ID', required: false }),
    };
    async run() {
        const { args, flags } = await this.parse(EmasserGetWorkflowInstances);
        const apiCxn = new api_connection_1.ApiConnection();
        const getWorkflowInstances = new emass_client_1.WorkflowInstancesApi(apiCxn.configuration, apiCxn.basePath, apiCxn.axiosInstances);
        if (args.name === 'all') {
            // Order is important here
            try {
                const response = await getWorkflowInstances.getSystemWorkflowInstances(flags.includeComments, flags.includeDecommissionSystems, flags.pageIndex, flags.sinceDate, flags.status);
                console.log((0, json_colorizer_1.colorize)((0, output_formatter_1.outputFormat)(response)));
            }
            catch (error) {
                (0, utilities_1.displayError)(error, 'Workflow Instances');
            }
        }
        else if (args.name === 'byInstanceId') {
            // Order is important here
            try {
                const response = await getWorkflowInstances.getSystemWorkflowInstancesByWorkflowInstanceId(flags.workflowInstanceId);
                console.log((0, json_colorizer_1.colorize)((0, output_formatter_1.outputFormat)(response)));
            }
            catch (error) {
                (0, utilities_1.displayError)(error, 'Workflow Instances');
            }
        }
        else {
            throw new Error(`Unexpected argument: ${args.name}`);
        }
    }
    catch(error) {
        if (error instanceof Error) {
            this.warn(error);
        }
        else {
            const suggestions = 'get workflow_instances [-h or --help]\n\tget workflow_instances all\n\tget workflow_instances byInstanceId';
            this.warn('Invalid arguments\nTry this 👇:\n\t' + suggestions);
        }
        return Promise.resolve();
    }
}
exports.default = EmasserGetWorkflowInstances;
//# sourceMappingURL=workflow_instances.js.map