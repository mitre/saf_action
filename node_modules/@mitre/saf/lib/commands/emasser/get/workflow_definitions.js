"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const emass_client_1 = require("@mitre/emass_client");
const core_1 = require("@oclif/core");
const json_colorizer_1 = require("json-colorizer");
const api_connection_1 = require("../../../utils/emasser/api_connection");
const output_formatter_1 = require("../../../utils/emasser/output_formatter");
const utilities_1 = require("../../../utils/emasser/utilities");
class EmasserGetWorkflowDefinitions extends core_1.Command {
    static usage = '<%= command.id %> [FLAGS]';
    static description = 'View all workflow schemas available on the eMASS instance';
    static examples = ['<%= config.bin %> <%= command.id %> [options]'];
    static flags = {
        help: core_1.Flags.help({ char: 'h', description: 'Show eMASSer CLI help for the GET Workflow Definitions command' }),
        ...(0, utilities_1.getFlagsForEndpoint)(process.argv),
    };
    async run() {
        const { flags } = await this.parse(EmasserGetWorkflowDefinitions);
        const apiCxn = new api_connection_1.ApiConnection();
        const getWorkflow = new emass_client_1.WorkflowDefinitionsApi(apiCxn.configuration, apiCxn.basePath, apiCxn.axiosInstances);
        // Order is important here
        try {
            const response = await getWorkflow.getWorkflowDefinitions(flags.includeInactive, flags.registrationType);
            console.log((0, json_colorizer_1.colorize)((0, output_formatter_1.outputFormat)(response)));
        }
        catch (error) {
            (0, utilities_1.displayError)(error, 'Workflow Definitions');
        }
    }
}
exports.default = EmasserGetWorkflowDefinitions;
//# sourceMappingURL=workflow_definitions.js.map