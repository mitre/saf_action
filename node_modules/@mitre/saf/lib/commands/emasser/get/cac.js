"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const json_colorizer_1 = require("json-colorizer");
const emass_client_1 = require("@mitre/emass_client");
const core_1 = require("@oclif/core");
const api_connection_1 = require("../../../utils/emasser/api_connection");
const output_formatter_1 = require("../../../utils/emasser/output_formatter");
const utilities_1 = require("../../../utils/emasser/utilities");
class EmasserGetCac extends core_1.Command {
    static usage = '<%= command.id %> [FLAGS]';
    static description = 'View one or many Control Approval Chain (CAC) in a system specified system ID';
    static examples = ['<%= config.bin %> <%= command.id %> [-s, --systemId] <value>'];
    static flags = {
        help: core_1.Flags.help({ char: 'h', description: 'Show eMASSer CLI help for the GET CAC command' }),
        ...(0, utilities_1.getFlagsForEndpoint)(process.argv),
    };
    async run() {
        const { flags } = await this.parse(EmasserGetCac);
        const apiCxn = new api_connection_1.ApiConnection();
        const getCac = new emass_client_1.CACApi(apiCxn.configuration, apiCxn.basePath, apiCxn.axiosInstances);
        // Order is important here
        try {
            const response = await getCac.getSystemCac(flags.systemId, flags.controlAcronyms);
            console.log((0, json_colorizer_1.colorize)((0, output_formatter_1.outputFormat)(response)));
        }
        catch (error) {
            (0, utilities_1.displayError)(error, 'CAC');
        }
    }
    catch(error) {
        if (error instanceof Error) {
            this.warn(error.message);
        }
        else {
            const suggestions = 'get cac [-h or --help]';
            this.warn('Invalid arguments\nTry this:\n\t' + suggestions);
        }
        return Promise.resolve();
    }
}
exports.default = EmasserGetCac;
//# sourceMappingURL=cac.js.map