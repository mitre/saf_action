import { ContextualizedControl, ControlStatus, HDFControlSegment, Severity } from 'inspecjs';
import { ChecklistControl } from '../types/checklist';
export declare function cklSeverity(severity: Severity): 'low' | 'medium' | 'high';
export declare function cklStatus(status: ControlStatus): string;
export declare function cklResults(segments?: HDFControlSegment[]): string;
export declare function getDetails(control: ContextualizedControl, profileName: string): ChecklistControl;
