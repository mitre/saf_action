"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.createWinstonLogger = createWinstonLogger;
const _ = __importStar(require("lodash"));
const winston_1 = require("winston");
// Use user defined colors. Used by the console log transporter
const syslogColors = {
    debug: 'blue',
    info: 'cyan',
    notice: 'white',
    warn: 'magenta',
    warning: 'bold magenta',
    error: 'bold red',
    verbose: 'blue',
    crit: 'inverse yellow',
    alert: 'bold inverse red',
    emerg: 'bold inverse magenta',
    prefix: 'yellow',
};
function createWinstonLogger(library = 'inspec-objects', level = 'debug') {
    const colorizer = winston_1.format.colorize({ colors: syslogColors });
    return (0, winston_1.createLogger)({
        level: level,
        transports: [new winston_1.transports.Console()],
        format: winston_1.format.combine(winston_1.format.timestamp({
            format: 'MMM-DD-YYYY HH:mm:ss Z',
        }), winston_1.format.errors({ stack: true }), winston_1.format.printf((info) => {
            const prefix = colorizer.colorize('prefix', `[${info.timestamp} -> ${library}]`);
            const lvl = colorizer.colorize(info.level, info.level);
            let msg = '';
            if (info.message) {
                msg = _.isString(info.message) ? info.message : JSON.stringify(info.message, null, 2);
            }
            if (info.stack) {
                msg += `\n${_.isString(info.stack) ? info.stack : JSON.stringify(info.stack, null, 2)}`;
            }
            msg = colorizer.colorize(info.level, msg);
            return `${prefix} - ${lvl}: ${msg}`;
        })),
    });
}
//# sourceMappingURL=logging.js.map