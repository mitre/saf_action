import { ConfigRule, ConfigService, ConfigServiceClientConfig } from '@aws-sdk/client-config-service';
import { ExecJSON } from 'inspecjs';
export declare class AwsConfigMapper {
    configService: ConfigService;
    issues: Promise<ConfigRule[]>;
    results: ExecJSON.ControlResult[][];
    constructor(options: ConfigServiceClientConfig, verifySSLCertificates?: boolean, certificate?: string);
    private delay;
    private getAllConfigRules;
    private getConfigRulePage;
    private getResults;
    private appendResourceNamesToResults;
    private extractResourceNamesFromIds;
    private getCodeDesc;
    private getRunTime;
    private getStatus;
    private getMessage;
    private fetchAllComplianceInfo;
    private hdfTags;
    private checkText;
    private hdfDescriptions;
    private getAccountId;
    private getControls;
    private getImpact;
    toHdf(): Promise<ExecJSON.Execution>;
}
