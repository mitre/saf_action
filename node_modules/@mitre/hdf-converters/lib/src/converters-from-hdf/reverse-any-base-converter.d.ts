import { MappedReform, ObjectEntryValue } from '../base-converter';
import { FromHdfBaseConverter, ILookupPathFH } from './reverse-base-converter';
export declare class FromAnyBaseConverter extends FromHdfBaseConverter {
    data: any;
    constructor(data: any, collapseResults?: boolean);
    convertInternal(file: object, fields: any): MappedReform<any, ILookupPathFH>;
    objectMap<T extends Array<unknown>, V>(obj: T, fn: (v: ObjectEntryValue<T>) => V): {
        [K in keyof T]: V;
    };
}
