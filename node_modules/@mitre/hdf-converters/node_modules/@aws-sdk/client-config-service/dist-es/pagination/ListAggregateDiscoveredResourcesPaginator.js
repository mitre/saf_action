import { createPaginator } from "@smithy/core";
import { ListAggregateDiscoveredResourcesCommand, } from "../commands/ListAggregateDiscoveredResourcesCommand";
import { ConfigServiceClient } from "../ConfigServiceClient";
export const paginateListAggregateDiscoveredResources = createPaginator(ConfigServiceClient, ListAggregateDiscoveredResourcesCommand, "NextToken", "NextToken", "Limit");
