import { createPaginator } from "@smithy/core";
import { SelectResourceConfigCommand, } from "../commands/SelectResourceConfigCommand";
import { ConfigServiceClient } from "../ConfigServiceClient";
export const paginateSelectResourceConfig = createPaginator(ConfigServiceClient, SelectResourceConfigCommand, "NextToken", "NextToken", "Limit");
