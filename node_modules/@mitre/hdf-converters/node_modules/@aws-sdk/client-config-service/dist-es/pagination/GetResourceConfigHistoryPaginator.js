import { createPaginator } from "@smithy/core";
import { GetResourceConfigHistoryCommand, } from "../commands/GetResourceConfigHistoryCommand";
import { ConfigServiceClient } from "../ConfigServiceClient";
export const paginateGetResourceConfigHistory = createPaginator(ConfigServiceClient, GetResourceConfigHistoryCommand, "nextToken", "nextToken", "limit");
