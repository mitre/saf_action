import { createPaginator } from "@smithy/core";
import { DescribeConfigurationAggregatorsCommand, } from "../commands/DescribeConfigurationAggregatorsCommand";
import { ConfigServiceClient } from "../ConfigServiceClient";
export const paginateDescribeConfigurationAggregators = createPaginator(ConfigServiceClient, DescribeConfigurationAggregatorsCommand, "NextToken", "NextToken", "Limit");
