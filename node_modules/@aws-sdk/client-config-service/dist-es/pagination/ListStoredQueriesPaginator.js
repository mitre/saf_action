import { createPaginator } from "@smithy/core";
import { ListStoredQueriesCommand, } from "../commands/ListStoredQueriesCommand";
import { ConfigServiceClient } from "../ConfigServiceClient";
export const paginateListStoredQueries = createPaginator(ConfigServiceClient, ListStoredQueriesCommand, "NextToken", "NextToken", "MaxResults");
