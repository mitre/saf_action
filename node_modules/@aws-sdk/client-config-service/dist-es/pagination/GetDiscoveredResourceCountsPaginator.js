import { createPaginator } from "@smithy/core";
import { GetDiscoveredResourceCountsCommand, } from "../commands/GetDiscoveredResourceCountsCommand";
import { ConfigServiceClient } from "../ConfigServiceClient";
export const paginateGetDiscoveredResourceCounts = createPaginator(ConfigServiceClient, GetDiscoveredResourceCountsCommand, "nextToken", "nextToken", "limit");
