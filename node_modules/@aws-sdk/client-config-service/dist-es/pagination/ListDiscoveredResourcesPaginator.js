import { createPaginator } from "@smithy/core";
import { ListDiscoveredResourcesCommand, } from "../commands/ListDiscoveredResourcesCommand";
import { ConfigServiceClient } from "../ConfigServiceClient";
export const paginateListDiscoveredResources = createPaginator(ConfigServiceClient, ListDiscoveredResourcesCommand, "nextToken", "nextToken", "limit");
