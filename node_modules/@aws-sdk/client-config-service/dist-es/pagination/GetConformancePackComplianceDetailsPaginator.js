import { GetConformancePackComplianceDetailsCommand, } from "../commands/GetConformancePackComplianceDetailsCommand";
import { ConfigServiceClient } from "../ConfigServiceClient";
const makePagedClientRequest = async (client, input, ...args) => {
    return await client.send(new GetConformancePackComplianceDetailsCommand(input), ...args);
};
export async function* paginateGetConformancePackComplianceDetails(config, input, ...additionalArguments) {
    let token = config.startingToken || undefined;
    let hasNext = true;
    let page;
    while (hasNext) {
        input.NextToken = token;
        input["Limit"] = config.pageSize;
        if (config.client instanceof ConfigServiceClient) {
            page = await makePagedClientRequest(config.client, input, ...additionalArguments);
        }
        else {
            throw new Error("Invalid client, expected ConfigService | ConfigServiceClient");
        }
        yield page;
        const prevToken = token;
        token = page.NextToken;
        hasNext = !!(token && (!config.stopOnSameToken || token !== prevToken));
    }
    return undefined;
}
