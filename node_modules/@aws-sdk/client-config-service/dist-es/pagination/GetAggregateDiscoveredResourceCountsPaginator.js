import { createPaginator } from "@smithy/core";
import { GetAggregateDiscoveredResourceCountsCommand, } from "../commands/GetAggregateDiscoveredResourceCountsCommand";
import { ConfigServiceClient } from "../ConfigServiceClient";
export const paginateGetAggregateDiscoveredResourceCounts = createPaginator(ConfigServiceClient, GetAggregateDiscoveredResourceCountsCommand, "NextToken", "NextToken", "Limit");
