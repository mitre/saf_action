import { marked } from 'marked';
import Tokens = marked.Tokens;
export declare class TypeToString {
    static link(tok: Tokens.Link, tag: string): string;
    static tableAlign(tok: Tokens.Table): string;
}
