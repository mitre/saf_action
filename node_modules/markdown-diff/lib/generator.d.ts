export declare class Generator {
    private static newLine;
    private rendered;
    /**
     * exec
     */
    exec(oldString: string, newString: string): string;
    private walkTokens;
    private handleToken;
}
