import { DiffState } from '../jsdiff.util';
export interface DiffEle {
    state: DiffState;
}
export declare class DiffString implements DiffEle {
    value: string;
    state: DiffState;
    constructor(value: string, state: DiffState);
}
export declare class BlockQuoteDiff implements DiffEle {
    state: DiffState;
    depth: number;
    value: string;
    constructor(value: string, depth: number, state: DiffState);
}
