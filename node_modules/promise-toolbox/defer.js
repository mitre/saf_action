"use strict";

var defer = function defer() {
  var resolve, reject;
  var promise = new Promise(function (resolve_, reject_) {
    resolve = resolve_;
    reject = reject_;
  });
  return {
    promise,
    reject,
    resolve
  };
};

module.exports = defer;